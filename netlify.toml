# Netlify build settings — lets the site deploy straight from the GitHub
# repo (no drag-and-drop needed): connect the repo, pick the default
# branch, and Netlify builds the game with these settings automatically.
[build]
  command = "npm run build"
  publish = "dist"
